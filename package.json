{
  "name": "opendesk",
  "private": true,
  "packageManager": "pnpm@9.12.0",
  "workspaces": [
    "backend",
    "web",
    "client",
    "host",
    "aplicativo"
  ],
  "scripts": {
    "dev": "concurrently -k -n backend,web,desktop -c blue,green,magenta \"pnpm --filter backend dev\" \"pnpm --filter web dev\" \"pnpm --filter aplicativo dev\"",
    "dev:desktop": "node scripts/dev-desktop.mjs",
    "lint": "pnpm --filter backend lint && pnpm --filter web lint",
    "format": "prettier --write .",
    "test": "pnpm --filter backend test"
  },
  "devDependencies": {
    "concurrently": "^8.2.2",
    "eslint": "^9.12.0",
    "prettier": "^3.3.3"
  }
}
